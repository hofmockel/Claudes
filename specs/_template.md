# [Feature Name]

<!-- One sentence: what this feature does and for whom. -->

## Overview

<!-- 2–4 sentences: why this feature exists, what problem it solves, key constraints. -->

## Behavior

<!-- What the system does from the user's or API caller's perspective.
     Present tense. No implementation details. -->

## Acceptance Criteria

<!-- Checkboxes. These are what /check-drift verifies. Be specific and testable.
     Each criterion should be independently verifiable. -->

- [ ] [Criterion: e.g. "User can log in with a valid email and password"]
- [ ] [Criterion: e.g. "Invalid credentials return 401 with error code INVALID_CREDENTIALS"]

## API / Interface

<!-- Endpoints, function signatures, CLI flags, data shapes. Be precise.

POST /example
Body: { field: type }
200:  { result: type }
400:  { error: "ERROR_CODE" }
-->

## Data Model

<!-- Tables, schemas, types relevant to this feature. -->

## Edge Cases

<!-- Inputs or states that require special handling. -->

- [e.g. "Email lookup is case-insensitive"]

## Out of Scope

<!-- Explicitly what this spec does NOT cover. Prevents scope creep. -->

- [e.g. "OAuth login — tracked in oauth.md"]
